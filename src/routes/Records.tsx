import { RoutePlaceholder } from '@/components/ui/RoutePlaceholder';

/** `/records`. */
export function Records() {
  return <RoutePlaceholder eyebrow="Records and cross-era" title="Records" ships="F8" />;
}
