import { RoutePlaceholder } from '@/components/ui/RoutePlaceholder';

/** `/drivers`. */
export function DriverIndex() {
  return <RoutePlaceholder eyebrow="Driver index" title="Drivers" ships="F4" />;
}
