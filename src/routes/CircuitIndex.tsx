import { RoutePlaceholder } from '@/components/ui/RoutePlaceholder';

/** `/circuits`. */
export function CircuitIndex() {
  return <RoutePlaceholder eyebrow="Circuit index" title="Circuits" ships="F6" />;
}
