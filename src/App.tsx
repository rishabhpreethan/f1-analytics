/**
 * Placeholder shell boot. The router and the eleven routes land in T10; this exists
 * only so T8's acceptance — a served page with a Tailwind utility visibly applied —
 * can be verified before the shell is built.
 */
export function App() {
  return (
    <main className="bg-surface-canvas text-ink-primary p-8">
      <h1 className="t-display-lg">F1 Analytics</h1>
      <p className="t-md text-ink-secondary">Foundation scaffold.</p>
      <p className="t-mono t-xs text-ink-tertiary">1:32.608</p>
    </main>
  );
}
